@model IEnumerable<SchoolbookApp.Models.Subject>
@{
    ViewData["Title"] = "TeacherProgram";
}

<head>

</head>
<body>
    <h1 id="weekly-program">Седмична програма на @ViewBag.TeacherName:</h1>
    <div class="weekly-program">
        <table class="table table-striped table-bordered table-light">
            <thead class="thead-dark">
                <tr>
                    <th colspan="3">Понеделник</th>
                </tr>
                <tr>
                    <th>Час</th>
                    <th>Клас</th>
                    <th>Стая</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var el in Model.ToList().OrderBy(x => x.Time))
                {
                    @if (el.Day == DayOfWeek.Monday)
                    {
                        <tr>
                            <td>@el.Time</td>
                            <td>@el.SchoolClass.Num @el.SchoolClass.Letter - @el.SubjectType.Name</td>
                            <td>@el.Room.Name</td>
                        </tr>
                    }

                }
            </tbody>
        </table>

        <table class="table table-striped table-bordered table-light">
            <thead class="thead-dark">
                <tr>
                    <th colspan="3">Вторник</th>
                </tr>
                <tr>
                    <th>Час</th>
                    <th>Клас</th>
                    <th>Стая</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var el in Model.ToList().OrderBy(x => x.Time))
                {
                    @if (el.Day == DayOfWeek.Tuesday)
                    {
                        <tr>
                            <td>@el.Time</td>
                            <td>@el.SchoolClass.Num @el.SchoolClass.Letter - @el.SubjectType.Name</td>
                            <td>@el.Room.Name</td>
                        </tr>
                    }

                }
            </tbody>
        </table>

        <table class="table table-striped table-bordered table-light">
            <thead class="thead-dark">
                <tr>
                    <th colspan="3">Сряда</th>
                </tr>
                <tr>
                    <th>Час</th>
                    <th>Клас</th>
                    <th>Стая</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var el in Model.ToList().OrderBy(x => x.Time))
                {
                    @if (el.Day == DayOfWeek.Wednesday)
                    {
                        <tr>
                            <td>@el.Time</td>
                            <td>@el.SchoolClass.Num @el.SchoolClass.Letter - @el.SubjectType.Name</td>
                            <td>@el.Room.Name</td>
                        </tr>
                    }

                }
            </tbody>
        </table>

        <table class="table table-striped table-bordered table-light">
            <thead class="thead-dark">
                <tr>
                    <th colspan="3">Четвъртък</th>
                </tr>
                <tr>
                    <th>Час</th>
                    <th>Клас</th>
                    <th>Стая</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var el in Model.ToList().OrderBy(x => x.Time))
                {
                    @if (el.Day == DayOfWeek.Thursday)
                    {
                        <tr>
                            <td>@el.Time</td>
                            <td>@el.SchoolClass.Num @el.SchoolClass.Letter - @el.SubjectType.Name</td>
                            <td>@el.Room.Name</td>
                        </tr>
                    }
                }
            </tbody>
        </table>

        <table class="table table-striped table-bordered table-light">
            <thead class="thead-dark">
                <tr>
                    <th colspan="3">Петък</th>
                </tr>
                <tr>
                    <th>Час</th>
                    <th>Клас</th>
                    <th>Стая</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var el in Model.ToList().OrderBy(x => x.Time))
                {
                    @if (el.Day == DayOfWeek.Friday)
                    {
                        <tr>
                            <td>@el.Time</td>
                            <td>@el.SchoolClass.Num @el.SchoolClass.Letter - @el.SubjectType.Name</td>
                            <td>@el.Room.Name</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</body>

